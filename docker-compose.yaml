services:
  web-app:
    container_name: tender-check
    build:
      context: .
      dockerfile: Dockerfile
    image: tender:v2.1
    restart: unless-stopped
    environment:
      TZ: Asia/Shanghai
    ports:
      - "30018:8000"
    volumes:
      - ./:/app/tender
